{
  "name": "first-next-js",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && touch ./out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "gh-pages": "^6.1.0",
    "next": "^13.4.19",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-feather": "^2.0.10",
    "touch-cli": "^0.0.1"
  },
  "devDependencies": {
    "@svgr/webpack": "^8.1.0",
    "sass": "^1.69.5"
  }
}
